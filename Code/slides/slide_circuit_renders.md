# SBOL Circuits Rendered from LoRA Output

All three are d5 (hardest tier) prompts, generated by Jetson UD-Q3_K_M + QLoRA at 7 tok/s, parsed and rendered as directed graphs.

## 1. CRISPRi inverter — score 94/100
![CRISPRi inverter](circuit_crispri_inverter.png)

**Prompt:** *"CRISPRi inverter: constitutive dCas9 with sgRNA targeting a pTarget drives GFP OFF when sgRNA is induced."*

Circuit topology correctly captures:
- Two transcription units (constitutive dCas9, inducible sgRNA)
- Both CDS activate the `operator_target`
- `operator_target` represses `p_target` (red dashed)
- `p_target` drives GFP only when sgRNA is *absent* → inverter logic

## 2. AHL quorum-sensing consensus — score 93/100
![QS consensus](circuit_qs_consensus.png)

**Prompt:** *"Population-level consensus circuit using AHL-mediated quorum sensing to synchronize GFP pulses across cells."*

Clean positive-feedback loop: `luxr_cds → p_lux → luxi_cds → p_lux` (AHL synthase amplifies the signal that activates its own promoter). GFP is transcribed from the same threshold-sensitive promoter → synchronized expression once population AHL crosses threshold.

This is the same prompt where the **bare model scored 0** (JSON comment injection).

## 3. Tumor-targeting Nissle circuit — score 93/100
![Tumor targeting](circuit_tumor_targeting.png)

**Prompt:** *"Engineer a tumor-targeting E. coli Nissle circuit: hypoxia-responsive FNR promoter drives InvA; constitutive anti-HER2 nanobody surface-displayed via INP anchor."*

Two independent transcription units:
- Hypoxia branch: `fnr_promoter → inva_cds` (tumor-hypoxia invasion)
- Targeting branch: `p_const → anti_her2_inp_fusion` (constitutive surface display)

Both units are properly terminated and ribosome-controlled.

## Why this matters

These are not textbook circuits scored against a memorized answer key.

- The rubric evaluates **structural validity** + **wiring correctness** + **biological plausibility** against machine-checked rules.
- Renders above are **automatically generated from the model's JSON**, with no human cleanup.
- All three compile, parse, and produce a coherent diagram.

The 4B-parameter QLoRA is generating **publication-quality schematics** of circuits it has never seen in training — rendered here by the same 100-line Python script that would drive a downstream SBOL-to-DNA compilation pipeline.
